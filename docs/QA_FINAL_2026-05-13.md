# QA finale Dream Digital - 2026-05-13

## Parcours couverts automatiquement

- Home FR/EN.
- Hubs marketing : produits, developers, solutions, coverage, pricing, company, contact.
- Page produit detaillee : SMS A2P.
- Blog index + article publie.
- Pages legales FR : mentions, CGU, RGPD.
- Admin CMS : revisions, media library, preview, duplication FR/EN.
- Performance assets : front minimal, pas de JS home sur pages produits/marketing.

## Commandes de verification

```powershell
php artisan test
npm run build:public
npm run build
php artisan route:cache
php artisan config:cache
php artisan dd:launch-check
```

## Resultats observes

- `php artisan test` : 140 tests passes / 565 assertions.
- `npm run build:public` : OK ; CSS front reduit via `front-core.scss` et subset Iconify.
- `npm run build` : OK ; warning attendu sur les gros chunks admin/demo.
- `php artisan dd:launch-check` : contenu business OK apres seed local, blocages attendus tant que l'environnement cible n'a pas owner/admin actif, legal name, support email et telephone.
- Les tests doivent etre lances avec la config Laravel non cachee ; apres verification, reconstruire `route:cache` et `config:cache` pour l'application.

## Points de vigilance humains

- Verifier visuellement le hero home desktop/mobile avant indexation.
- Relire les claims business : SLA, pays, tarifs indicatifs, support, legal.
- Ne pas activer `DD_PUBLIC_INDEXABLE=true` avant `php artisan dd:launch-check --public` OK en production.
- Le build complet garde un warning gros chunks admin/demo ; le build public reste l'artefact pertinent pour la vitrine.
